/**
 * `openapi-typescript` によって `openapi/openapi.yaml` から自動生成されるファイル。
 * 手で編集しないこと。
 *
 * 生成コマンド: `pnpm run api:generate`
 *
 * 初期状態ではスキーマが最小構成のため、このファイルは一時的な placeholder として
 * 空の型エイリアスだけを export している。OpenAPI の定義が増えたら上書き再生成される。
 */
// biome-ignore lint/complexity/noBannedTypes: placeholder until real schema is generated
export type paths = {};
// biome-ignore lint/complexity/noBannedTypes: placeholder until real schema is generated
export type components = {};
// biome-ignore lint/complexity/noBannedTypes: placeholder until real schema is generated
export type operations = {};
