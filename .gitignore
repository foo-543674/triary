# ---------------- Rust / Backend ----------------
backend/target/
**/*.rs.bk
*.pdb
# NOTE: We commit the sqlx offline query cache by default, so do not ignore
#       it here. Uncomment the line below only if you decide not to commit it.
# backend/.sqlx/

# ---------------- Node / Frontend ----------------
frontend/node_modules/
frontend/dist/
frontend/.vite/
frontend/coverage/
frontend/storybook-static/
frontend/playwright-report/
frontend/test-results/
frontend/.turbo/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
.pnpm-store/

# ---------------- Tests / coverage ----------------
backend/coverage/
backend/tarpaulin-report.html
lcov.info
*.profraw

# ---------------- Local infra / Docker ----------------
# NOTE: Temporary files generated by docker compose.
docker-compose.override.yml
# NOTE: MySQL dumps written to the host filesystem.
*.sql.gz
*.dump

# ---------------- OpenAPI / generated artifacts ----------------
# NOTE: Files generated from the OpenAPI schema (types, SDK, HTML docs, ...).
openapi/generated/
docs/api/generated/

# ---------------- IDE / editor ----------------
.vscode/
!.vscode/extensions.json
!.vscode/settings.shared.json
.idea/
*.swp
*.swo
*.swn
*~

# ---------------- OS ----------------
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db
Desktop.ini

# ---------------- Environment variables / secrets ----------------
.env
.env.*
!.env.example
*.pem
*.key
!**/test/**/*.pem
!**/test/**/*.key

# ---------------- Claude Code (per-user settings only) ----------------
.claude/settings.local.json
.claude/.credentials.json

# ---------------- Logs / temp files ----------------
*.log
logs/
tmp/
.tmp/
.cache/
